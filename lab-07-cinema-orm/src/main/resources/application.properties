spring.datasource.url=jdbc:postgresql://localhost:5432/cinemalab
spring.datasource.username=postgres
spring.datasource.password=Giorgi2008!
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

#spring.flyway.baseline-on-migrate=true
#where fly creates table? If we say true, then spring knows that it will create another table based on DataSource.URL we put on top of configuration

#spring.flyway.enabled=false
#whenever we put enable false, we tell do not look DB information in the app properties. Look Runner class instead.
#We tell here flyway, do not look for dataSource in application properties (as it executes first), we tell instead go and check runner class.
